default_shell "zsh"
pane_frames true
simplified_ui false
theme "default"

keybinds clear-defaults=false {
    normal {
        bind "Ctrl g" { SwitchToMode "locked"; }
    }
    locked {
        bind "Ctrl g" { SwitchToMode "normal"; }
    }
}
